<h1>My Tasks</h1>
  <ul>
    <% @tasks.each do |task|%>
      <% if task.completed %>
          <li>
          <div class="small-container">
            <i class="fas fa-check-square"></i>
            <%= link_to task.title, task_path(task)%>
          </div>
          <%= link_to "🗑", task_path(task), method: :delete, data: { confirm: "Are you sure you to delete this task?" }%>
          </li>
      <% else %>
          <li>
          <div class="small-container">
            <i class="fas fa-square"></i>
            <%= link_to task.title, task_path(task)%>
          </div>
          <%= link_to "🗑", task_path(task), method: :delete, data: { confirm: "Are you sure you want to delete this task?" }%>
          </li>
      <% end %>
    <% end %>
 </ul>
<%= link_to (new_task_path) do %>
  <i class="fas fa-plus"> Add a new task</i>
<% end %>
